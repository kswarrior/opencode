/* Minimal vendored htmx stub for ksx offline builds (<1KB).
 * Replace with the official htmx build for production:
 *   curl -sL https://unpkg.com/htmx.org@2 -o assets/htmx.min.js
 * ksx uses vanilla WebSocket + insertAdjacentHTML (htmx-style swap),
 * so this stub only provides a version marker + no-op helpers. */
window.htmx = window.htmx || { version: "ksx-stub-1", config: {}, on: function(){}, trigger: function(){} };
console.info("[ksx] htmx stub loaded — swap via WSS insertAdjacentHTML");
