# binaries
*/server
server
*.o
# env — root .env local only, but per-service .env are committed for individual Render deploys
.env
!main/.env
!auth/.env
!account/.env
!.env.example
# docker
.docker/
# os
.DS_Store
